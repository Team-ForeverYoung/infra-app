apiVersion: v1
kind: Service
metadata:
  name: {{ include "us-bechart.fullname" . }}
  labels:
    {{- include "us-bechart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "us-bechart.selectorLabels" . | nindent 4 }}
