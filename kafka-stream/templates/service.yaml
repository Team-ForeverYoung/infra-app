apiVersion: v1
kind: Service
metadata:
  name: {{ include "kafka-stream.fullname" . }}
  labels:
    {{- include "kafka-stream.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kafka-stream.selectorLabels" . | nindent 4 }}
